const Backend = () => {
  return (
    <section className="normal-section">
      <h1>Backend Development</h1>
      <p>
        Building secure servers, APIs and databases using Django, Node.js, and MySQL.
      </p>
    </section>
  );
};

export default Backend;